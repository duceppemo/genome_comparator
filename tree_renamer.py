#!/usr/bin/env python3
"""Kept for backward compatibility. Prefer the installed `tree-renamer` command."""

from genome_comparator.cli import rename_main

if __name__ == '__main__':
    rename_main()
